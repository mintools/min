{"results":
    [
    #{list requirements, as: "requirement"}
       {
            "id" : "${requirement.id}",
            "label" : "${requirement.label.escapeJSON()}"
       }
       #{if !requirement_isLast},#{/if}
    #{/list}
    ]
}

